import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  list: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  stampContainer: {
    borderWidth: 1,
    borderRadius: 550,
    height: 50,
    width: 50,
    margin: 8,
    alignItems: "center",
    justifyContent: "center",
  },
  checked: { fontSize: 40, fontWeight: 200 },
});

export default styles;
